version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - errorlint
    - gosec
    - govet
    - ineffassign
    - misspell
    - predeclared
    - staticcheck
    - unused
  settings:
    errcheck:
      check-type-assertions: true

formatters:
  enable:
    - gofmt
